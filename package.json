{
  "name": "criptobot",
  "version": "1.0.0",
  "description": "A Crypto Currency Trading Bot",
  "main": "index.js",
  "scripts": {
    "prod": "NODE_ENV=production NODE_PATH=src node src/index.js",
    "start": "NODE_PATH=src node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mysql": "^2.17.1",
    "node-binance-api": "^0.9.9"
  }
}
